server:
  port: 8083

spring:
  application:
    name: employee-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/employee_db
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/hr-realm

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    preferIpAddress: true

